/**
 * PLACEHOLDER - this file is generated.
 *
 * Run the API, then from the repo root:
 *
 *     npm run gen:types
 *
 * That overwrites this file with types derived from FastAPI's OpenAPI document.
 * Re-run it whenever a Pydantic schema in services/api changes.
 */

export type paths = Record<string, unknown>
export type components = Record<string, unknown>
export type operations = Record<string, unknown>
