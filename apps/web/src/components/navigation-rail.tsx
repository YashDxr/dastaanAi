const links = [['Dashboard', '/dashboard', '◈'], ['Library', '/library', '▤'], ['Observability', '/observability', '⌁'], ['Settings', '/settings', '⚙']]
export function NavigationRail({ path, go }: { path: string; go: (to: string) => void }) { return <aside className="navigation-rail"><button className="wordmark" onClick={() => go('/')}><i>⌁</i> Daastaan<sup>AI</sup></button><nav aria-label="Primary">{links.map(([label, to, icon]) => <button key={to} className={path === to ? 'active' : ''} onClick={() => go(to)}><span>{icon}</span>{label}</button>)}</nav><button className="new-story" onClick={() => go('/upload')}>＋ <span>New story</span></button></aside> }
